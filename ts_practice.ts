// ------------------------------------------------------------
// ctrl + opt + N => to run the code
// tsc -w => to watch changes in all ts file
// tsc filename.ts -w => to watch changes in a specific ts file
// ------------------------------------------------------------

let number = 10;
while (number > 0) {
	console.log(--number);
}
